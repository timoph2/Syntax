
function multiplication_table(n) {
let multiplier = 1

    while (multiplier < 11){
    let result = multiplier * n
        console.log(`${multiplier} x ${n} = ${result}`)
        multiplier += 1
    }}

multiplication_table(7)

